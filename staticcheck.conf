# staticcheck configuration.
#
# SA1019 (use of a deprecated API) is excluded because the provider deliberately
# relies on a few deprecated APIs the ecosystem still ships — the
# controller-runtime event recorder (GetEventRecorderFor), the scheme.Builder
# helper, and apimachinery's wait.ErrWaitTimeout — until their upstream
# replacements are adopted. Everything else is enforced.
checks = ["all", "-SA1019"]
